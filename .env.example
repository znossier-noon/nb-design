# Tina Cloud: create a project at https://app.tina.io and connect this repo.
# Required for /admin editing in production and for TinaCloud to commit content to GitHub.
NEXT_PUBLIC_TINA_CLIENT_ID=
TINA_TOKEN=

# Optional: override the branch Tina indexes (defaults to VERCEL_GIT_COMMIT_REF or main).
# NEXT_PUBLIC_TINA_BRANCH=main
